# Hosted Supabase auth providers must also be configured for Google and Apple.
# Allow the redirect URL returned by getSocialAuthRedirectUrl() in lib/social/auth.ts.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
